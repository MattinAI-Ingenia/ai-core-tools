.DS_Store
.env
.env.local
.env.production
.env.*.local
.flaskenv
*.pyc
*.pyo
*.wav
ictvenv/
env/
*venv/
.venv/
dist/
build/
*.egg
*.egg-info/
.tox/
.cache/
.pytest_cache/
.idea/
docs/_build/
.vscode
uploads/
static/img/heroes/*
*node_modules*
#static/vendor/*
app/data
app/demo
# Coverage reports
htmlcov/
.coverage
.coverage.*
*,cover
cosas.txt
flask_session
__pycache__
downloads/
images/
agent_process.log
IA-CORE
data/
**/secrets.yaml
**/secret.yaml
*-secret.yaml
*.code-workspace
logs/
local/
clients/
qdrant_storage/
.playwright-mcp/
.claude/
temp/
.serena
tmpclaude*
plans/

nginx/
develop/
reports/

# Docker local overrides (never commit, use .env for secrets)
docker/docker-compose.override.yml

# Claude Code agent system: ship the agents/commands/skills/hooks,
# but keep per-run artifacts and personal settings out of git.
.claude/specs/
.claude/agent-memory/
**/.claude/agent-memory/
.claude/settings.local.json

# Spec-kit
.github/agents/speckit.*
prompts/
.specify/
specs/

# RAG eval benchmark: datasets, run results, and reports — not versioned
benchmark/

# Infinity model weights (reranker/embeddings) — GBs of downloaded checkpoints
docker/utilities/*/hf_cache/
.worktrees/

# Codex agent system: ship AGENTS.md, .codex/agents and .agents/skills,
# but keep per-run specs and personal config out of git.
.codex/specs/
.codex/config.local.toml

# Entornos Docker locales: un .env por entorno, con secretos. Las plantillas
# .env.example sí se versionan.
docker/envs/*.env
!docker/envs/*.env.example

# Dumps de BD locales (docker/mattin.ps1). Nunca al repo.
docker/backups/

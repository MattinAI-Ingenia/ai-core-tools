# ============================================================================
# __NAME__ — cliente con versión pineada.
# Creado con: .\mattin.ps1 new __NAME__ -Type cliente
#
# Poner en IMAGE_TAG el tag EXACTO que corre en el cliente y no tocarlo salvo
# decisión explícita de actualizarle: el arranque del backend hace
# `alembic upgrade head`, así que una imagen más nueva le migra la BD.
#
#   .\mattin.ps1 up __NAME__
# ============================================================================
COMPOSE_PROJECT_NAME=mattin-__NAME__
HTTP_PORT=__PORT__
FRONTEND_URL=http://localhost:__PORT__

IMAGE_TAG=CAMBIAME-sha-XXXXXXX
TEST_DB_PORT=__TESTPORT__

LANGSMITH_PROJECT=mattin-__NAME__

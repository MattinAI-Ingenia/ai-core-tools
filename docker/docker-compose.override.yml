# Personal local overrides — not tracked in git.
# Auto-loaded by `docker compose` alongside docker-compose.yaml.
services:
  postgres:
    ports:
      - "5434:5432"

  backend:
    environment:
      AICT_DEV_SEED_USERS: ${AICT_DEV_SEED_USERS:-}
